node_modules
# Keep environment variables out of version control
.env
uploads
generated

# Рабочие деревья других проектов (git worktree). Внутри бывает полный
# чекаут с node_modules на сотни мегабайт — ни в коммит, ни в архив выкладки.
.worktrees/

# prisma/seed-data.json НЕ игнорируется намеренно: это снимок данных сайта,
# из которого сидится прод. Без него `npm run seed` на сервере не отработает.
