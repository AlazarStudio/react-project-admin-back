-- Отдельный флаг «показывать в блоке на главной».
-- Аддитивно, DEFAULT true: все существующие вопросы остаются на главной,
-- видимость на сайте миграция не меняет. published по-прежнему главнее —
-- снятая публикация убирает вопрос отовсюду независимо от этого флага.

-- AlterTable
ALTER TABLE "faq_items" ADD COLUMN     "show_on_home" BOOLEAN NOT NULL DEFAULT true;
